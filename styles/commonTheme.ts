const fontSize = {
  xs: "0.75rem",
  sm: "0.875rem",
  base: "1rem",
  lg: "1,125rem",
  xl: "1.25rem",
  h2: "1.75rem",
};

const commonTheme = {
  fontSize,
};

export type FontSize = typeof fontSize;

export default commonTheme;
